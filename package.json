{
  "name": "einvoiceatlas",
  "type": "module",
  "version": "0.1.0",
  "description": "Global Electronic Invoice Regulatory Intelligence",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check:content": "node scripts/check-content.mjs",
    "verify": "npm run build && npm run check:content && node scripts/check-release-gate.mjs",
    "verify:live": "node scripts/check-live-production.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^6.4.2",
    "tailwindcss": "^4.3.0"
  }
}
