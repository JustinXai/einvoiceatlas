fix: resolve Quick Answers blank rendering and add QA release gates

Root causes:
- Bug A: QuickAnswerBox used self-closing <dd set:html /> which Astro
  silently drops in static build output
- Bug B: [slug].astro pages mapped {question,answer} to {q,a} but
  QuickAnswerBox props expect {question,answer}, causing all content
  to be undefined

Changes:
- QuickAnswerBox: replace set:html with plain text interpolation
- standards/[slug].astro, routes/[slug].astro: remove wrong .map()
  transform; pass {question,answer} directly
- check-release-gate.mjs: add checkQuickAnswers() parsing .qa-item
  count, dt non-empty, dd visible-text non-empty per target page
- check-live-production.mjs: same QA checks against live production
- standards.ts: fix 3 overclaiming regulatory phrases to cautious
  language (legally relevant vs compliant; Directive semantic model;
  ERP ecosystem wording)

Fixes QA item rendering for /standards/peppol-bis-3/ (5 items),
/standards/en-16931/ (5 items), /routes/peppol-access-point/ (4 items).
